# sentry-error-lab

Odoo 18 addon repository with one module, [`sentry_error_lab`](sentry_error_lab/README.md):
deliberate failures on demand to exercise a Sentry setup.

Development goes through branches and pull requests; CI runs ruff, a byte-compile
pass and XML validation.
